document.addEventListener('DOMContentLoaded', () => {
    console.log('Hello World app initialized');
    
    // Add dynamic cache busting for future asset loads if needed
    const version = Date.now();
    
    // Subtle interaction or logic could go here
    // For digital signage, we mainly ensure everything is rendered correctly
});
